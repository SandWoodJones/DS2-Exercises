#ifndef RECURSION_DS2
#define RECURSION_DS2

#include "ds2-exercises.h"

#include <stdbool.h>

extern int sum_interval (int, int);

extern unsigned int factorial (unsigned int);

unsigned int exponentiation (int, unsigned int);

int linear_search (int *, int, unsigned int, unsigned int);

void invert_array (int *, unsigned int, unsigned int);

bool is_palindrome (char *, int, int);

unsigned int find_smallest (int *, unsigned int, unsigned int);

#endif
